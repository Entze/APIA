stages:
  - draw.io graphs
  - graphs
  - pdf

Graphviz figures:
  stage: graphs
  image: alpine
  needs:
  script:
    - apk add make graphviz
    - make build-graphviz

  artifacts:
    paths:
      - 'Figures/**/*.pdf'

PlantUML figures:
  stage: graphs
  image:
    name: think/plantuml
    entrypoint:
      - ''
  needs:
  script:
    - apk add make
    - make build-plantuml

  artifacts:
    paths:
      - 'Figures/**/*.pdf'

Draw.io figures:
  stage: draw.io graphs
  image:
    name: rlespinasse/drawio-export
    entrypoint:
      - ''
  variables:
    DRAWIO_EXPORT_FILEEXT: 'svg'
    DRAWIO_EXPORT_FOLDER: '.'
  script:
    - /drawio/entrypoint.sh
  artifacts:
    paths:
      - 'Figures/**/*.svg'

SVG figures:
  stage: graphs
  image: alpine:3.12
  needs:
    - Draw.io figures
  script:
    - apk update
    - apk add make inkscape
    - make build-svg
  artifacts:
    paths:
      - 'Figures/**/*.pdf'

PDF:
  stage: pdf
  image: blang/latex:ctanfull
  needs:
    - PlantUML figures
    - Graphviz figures
    - Draw.io figures
    - SVG figures
  script:
    - latexmk -pdf

  artifacts:
    paths:
      - '*.pdf'
