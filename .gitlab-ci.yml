Build Graphviz figures:
  image: alpine
  script:
    - apk add make graphviz
    - make build-graphviz

  artifacts:
    paths:
      - 'Figures/**/*.pdf'

Build PlantUML figures:
  image:
    name: think/plantuml
    entrypoint:
      - ''
  script:
    - apk add make
    - make build-plantuml

  artifacts:
    paths:
      - 'Figures/**/*.pdf'

Build PDF:
  image: blang/latex:ctanfull
  needs:
    - Build PlantUML figures
    - Build Graphviz figures
  script:
    - latexmk -pdf

  artifacts:
    paths:
      - '*.pdf'
